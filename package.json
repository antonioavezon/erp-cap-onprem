{
  "name": "erp-cap-onprem",
  "version": "1.0.0",
  "description": "ERP CAP on-prem educational backend",
  "dependencies": {
    "@cap-js/postgres": "^2.0.6",
    "@sap/cds": "^9",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2"
  },
  "scripts": {
    "start": "cds-serve",
    "deploy": "cds-deploy"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": {
        "kind": "postgres",
        "impl": "@cap-js/postgres"
      }
    }
  }
}
